首次 

git clone 仓库b地址

例如    git clone https://github.com/pigonedragon001/coffee


cd coffee

// 修改了文件

git status    //  查看当前的状态

git add .

git commit -m "增加了一个模块"

git pull origin master    //  从服务端把最新的代码拽下来 确保 本地是最新的代码

git push origin master   // 提交到服务器端的仓库

git pull origin master    //  从服务端把最新的代码拽下来

